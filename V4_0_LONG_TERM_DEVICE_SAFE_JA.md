# v4.0 長期運用・端末引き継ぎ安全版

## 勤務校
- ログイン画面に `main` を固定表示しません。
- 学校コードは、その端末で最初にログインするときだけ入力します。
- ログイン成功後、その端末に勤務校コードだけを記憶し、次回から自動表示します。
- 異動時は「勤務校を変更」を押して新しい学校コードを入力します。
- 新しいiPadでは保存情報がないため、異動先の学校コードを最初の1回だけ入力します。

## 端末の引き継ぎ
教師画面に「異動・端末引き継ぎ」を追加しました。
- 教師情報だけ消す
- 児童用の端末データだけ消す
- この端末を全部初期化

初期化で消えるのはそのブラウザ内の `kq.*` データとキャッシュです。
Neon上の年度内学習履歴・教師アカウントそのものは削除しません。

## 児童側
- 学校用URL `/?school=学校コード` で学校を端末に設定します。
- 学校コードが未設定なら、児童コード登録を開始できません。
- 教師おためしサンドボックスはv3.8.2の合格仕様を維持します。

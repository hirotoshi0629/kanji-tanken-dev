# v3.0 初回設定
実名は保存しません。児童コード（例 3-1-07）のみです。

Vercel環境変数: SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / TEACHER_PASSWORD / TEACHER_TOKEN_SECRET

Supabase SQL Editorで SUPABASE_SCHEMA.sql を一度実行します。
先生画面は /teacher.html です。
学校・自治体のクラウド利用方針を確認してから本運用してください。

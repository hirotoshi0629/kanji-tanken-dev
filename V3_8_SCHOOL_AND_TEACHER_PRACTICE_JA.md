# v3.8 学校分離 + 教師おためしモード

## 追加
- 学校コードで教師・児童・学習履歴を完全分離。既存データは `main` 学校として引き継ぎ。
- 教師ログインに学校コードを追加。現在の学校は `main` のまま利用可能。
- 管理者画面に、その学校専用の児童URLを表示。将来別学校では `/?school=学校コード` を配布。
- 教師画面に「✏️ 問題をためす」を追加。児童版と同じ問題を解ける。
- おためしモードの結果は `/api/activity` に送信しないため、児童履歴・クラス集計には一切混ざらない。
- おためし画面には「先生用画面に戻る」を表示。

## Neon
この版では `NEON_SCHEMA.sql` の再実行が必要です。既存データは削除せず `school_code=main` で移行します。
